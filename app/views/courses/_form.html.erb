



<%= form_with(model: course, local: true) do |form| %>

 <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
        <% course.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

      <% for attribute in course.attributes.keys %>
          <div class="field">
            <%= form.label attribute.humanize.downcase %>
            <%= form.text_field attribute.humanize.downcase, :value => course.attributes[attribute].to_s %>
          </div>
      <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>

<% end %>













<% if false %>

<%= form_with(model: course, local: true) do |form| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
        <% course.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :school %>
    <%= form.collection_select :school, School.all, :id, :name_with_location, prompt: true %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="field">
    <%= form.label :specialprice %>
    <%= form.text_field :specialprice %>
  </div>

  <div class="field">
    <%= form.label :specialdue %>
    <%= form.datetime_select :specialdue %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :admintext %>
    <%= form.text_field :admintext %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>


<% end %>